

.chat-side
  .side-header
    .header__name
      = current_user.name
    %ul.header__lists
      %li.list_edit
        = link_to new_group_path do
          = icon('fa', 'edit', class: 'icon')
      %li.list_cog
        = link_to edit_user_path(current_user) do
          = icon('fa', 'cog', class: 'icon')



  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group) do
          %p.group__name
            = group.name
          %p.group__message
            = group.show_last_message
  
  -#  Prefix名 group＿messages+_path+(group)は
    -# messagesの#indexアクション（rails routesで確認)
    -# view/messages/index.html.hamlには全画面表示の記述